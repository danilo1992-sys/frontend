services:
  - type: web
    name: frontend
    runtime: node
    plan: free
    buildCommand: pnpm install && pnpm build
    startCommand: pnpm preview
    healthCheckPath: /
    envVars:
      - key: NODE_ENV
        value: production
